run = "bash start-replit.sh"
entrypoint = "start-replit.sh"

[nix]
channel = "stable-24_05"

[[ports]]
localPort = 8000
externalPort = 80

[deployment]
deploymentTarget = "cloudrun"
run = ["sh", "-c", "bash start-replit.sh"]
